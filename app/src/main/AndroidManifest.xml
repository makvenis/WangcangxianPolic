<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.makvenis.dell.wangcangxianpolic">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 定位权限 开始 -->
    <!-- 这个权限用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 这个权限用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SD卡读取权限，用户写入离线定位数据 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <!-- 定位权限 结束 -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="jqRvWsGCMomXuQ6erQgjPGzLVmqF0xVR" />

        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:process=":remote" />

        <activity android:name=".StartActivity" />
        <activity android:name=".startActivity.AdvertisingActivity" />
        <activity android:name=".startActivity.HomeActivity" />
        <activity android:name=".startActivity.GuideActivity" />
        <activity
            android:name=".startActivity.MainActivity"
            android:icon="@drawable/icon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".startActivity.BaseActivity" />
        <activity android:name=".login.UserLoginActivity" />
        <activity android:name=".login.RegisteActivity" />
        <activity android:name=".company.CompanyActivity" />
        <activity android:name=".company.CompanyItemDetailsActivity" />
        <activity android:name=".hotelcheck.HotelCheckActivity" />
        <activity android:name=".hotelcheck.NotesActivity" />
        <activity android:name=".newsnotescheck.NotesNewActivity" />
        <activity android:name=".startActivity.WebViewActivity" />
        <activity android:name=".correctActivity.CorrectCommandActivity" />
        <activity android:name=".activity.AlertPushNewsActivity" />
        <activity android:name=".startActivity.NotiflyActivity" />

        <service
            android:name=".service.SimpleServiceMessage"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activity.AlertPushPoliceActivity" />
        <activity android:name=".startActivity.WebHtmlActivity" />
        <activity android:name=".startActivity.WebPostRemarkActivity" />
        <activity android:name=".startActivity.TestActivity" />
        <activity android:name=".otherActivity.CorrectHistoryActivity" />
        <activity android:name=".otherActivity.SetActivity"></activity>
        <activity android:name=".minFragment.PersonalCenterActivity"></activity>
    </application>

</manifest>